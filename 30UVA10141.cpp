#if __APPLE__
    #include <iostream>
    #include <vector>
    #include <map>
    #include <set>
    #include <string>
    #include <algorithm>
#else
    #include <bits/stdc++.h>
#endif
using namespace std;
#define ll long long
#define xll __int128
#define pii pair<int,int>
#define pll pair<ll,ll>
#define v(type) vector<type>
#define MATRIX(type) v(v(type))
#define MAX(A,B,C) max(A,max(B,C))
#define MIN(A,B,C) min(A,min(B,C))
#define FASTIO ios_base::sync_with_stdio(false);cin.tie(NULL)
#define PREC 1e-9
int main(){
    // int n,p;
    // int rfp=1;
    // vector<string> res;
    // while(cin >> n >> p && n != 0 && p != 0){
    //     string str;
    //     cin.ignore();
    //     for(int i=0;i<n;++i){
    //         getline(cin, str);
    //     }
    //     string maxStr;
    //     int maxComp = 0;
    //     double maxPrice = 0;
    //     for(int i=0;i<p;++i){
    //         string str;
    //         double price;
    //         int rfp;
    //         getline(cin,str);
    //         cin >> price >> rfp;
    //         cin.ignore();
    //         for(int j=0;j<rfp;++j){
    //             string fake;
    //             getline(cin,fake);
    //         }
    //         if(maxStr == "" || (maxComp < rfp) || (maxComp == rfp && price < maxPrice)){
    //             maxStr = str;
    //             maxPrice = price;
    //             maxComp = rfp;
    //         }
    //     }
    //     res.push_back(maxStr);
    // }
    // for(int i=0;i<res.size();++i){
    //     cout<<"RFP #"<<i<<'\n';
    //     cout<<res[i]<<"\n";
    //     if(i < res.size()-1)
    //         cout<<'\n';
    // }
    `    
}
